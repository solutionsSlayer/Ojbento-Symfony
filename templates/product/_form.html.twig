{{ form_start(form) }}
    <div class="m-3">
      {{ form_widget(form) }}
    </div>
    <button class="btn btn-danger btn-rounded mr-2 mb-3">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
