{% extends 'base.html.twig' %}

{% block title %}Time{% endblock %}

{% block body %}
    <h1>Time</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ time.id }}</td>
            </tr>
            <tr>
                <th>Hour_command</th>
                <td>{{ time.hourCommand }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('time_index') }}">back to list</a>

    <a href="{{ path('time_edit', {'id': time.id}) }}">edit</a>

    {{ include('time/_delete_form.html.twig') }}
{% endblock %}
