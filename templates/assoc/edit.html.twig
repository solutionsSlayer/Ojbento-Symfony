{% extends 'base.html.twig' %}

{% block title %}Edit Assoc{% endblock %}

{% block body %}
    <h1>Edit Assoc</h1>

    {{ include('assoc/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('assoc_index') }}">back to list</a>

    {{ include('assoc/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    <script src ="{{ (asset('asset/js/assoc.js')) }}" type="text/javascript"></script>
    <script>
        $(document).ready(function(){
            $('#assoc_forMenu').val('0');
        })
    </script>
{% endblock %}
