<form method="post" action="{{ path('allergen_delete', {'id': allergen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allergen.id) }}">
    <button class="btn btn-danger btn-rounded btn-sm my-0">Supprimer</button>
</form>
