{% extends 'base.html.twig' %}

{% block title %}Pricetype{% endblock %}

{% block body %}

<div class="card">
  <h1 class="card-header text-center font-weight-bold text-uppercase py-4">Type de prix</h1>
  <div class="card-body">
    <div id="table" class="table-editable">
      <span class="table-add float-right mb-3 mr-2"><a href="#!" class="text-success"><i
            class="fas fa-plus fa-2x" aria-hidden="true"></i></a></span>
      <table class="table table-bordered table-responsive-md table-striped text-center">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pricetype.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pricetype.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex">
        <a class="d-flex justify-content-center align-items-center btn btn-danger btn-rounded mr-2" href="{{ path('pricetype_index') }}">Revenir à la liste</a>

        <a class="d-flex justify-content-center align-items-center btn btn-danger btn-rounded mr-2" href="{{ path('pricetype_edit', {'id': pricetype.id}) }}">Editer</a>

        <a style="color: white;" class="d-flex justify-content-center align-items-center btn btn-danger btn-rounded mr-2">{{ include('pricetype/_delete_form.html.twig') }}</a>
    </div>
{% endblock %}